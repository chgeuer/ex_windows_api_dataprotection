# Untitled notebook

```elixir
Mix.install([
  {:dpapi, path: "C:\\github\\chgeuer\\Windows-DPAPI-with-Zig\\elixir"}
])
```

## Section

```elixir
"#{System.get_env("USERPROFILE")}\\.azure\\msal_token_cache.bin"
|> File.read!()
|> DPAPI.unwrap()
|> IO.puts()
```
